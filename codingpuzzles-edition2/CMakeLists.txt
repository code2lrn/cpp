# Accepts a paragraph and prints out the groups of words that are anagrams
#
set(SOURCE_FILES anagramgrp.cpp)
add_executable(anagramgrp ${SOURCE_FILES})

set(SOURCE_FILES binarytree.cpp)
add_executable(binarytree ${SOURCE_FILES})