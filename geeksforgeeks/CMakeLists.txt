# Simple JSON Pretty Print
#
set(SOURCE_FILES jsonprint.cpp)
add_executable(jsonprint ${SOURCE_FILES})

# http://www.geeksforgeeks.org/stock-buy-sell/
# Determine best price to buy and sell stock in a month
#
set(SOURCE_FILES stockbuysell.cpp)
add_executable(stockbuysell ${SOURCE_FILES})

